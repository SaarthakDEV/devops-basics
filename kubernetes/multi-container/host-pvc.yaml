apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: host-volume-claim
spec: 
  volumeName: host-volume
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi